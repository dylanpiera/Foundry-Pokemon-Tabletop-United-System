<form class="contested-check" autocomplete="off" onsubmit="event.preventDefault();">
    {{!-- <p class="instructions">{{localize "PTU.Dialog.ContestedCheck.Hint"}}</p> --}}
    <div class="form-group">
        <label for="skill">{{localize "PTU.Dialog.ContestedCheck.Skill"}}</label>
        <div class="form-fields">
            <select name="skill">
                {{selectOptions skillOptions selected=skill}}
                {{!-- {{#select skill}}
                    {{#each skills as |skill|}}
                        <option value="{{skill}}">{{localize (concat "PTU.Skills." skill)}}</option>
                    {{/each}}
                {{/select}} --}}
            </select>
        </div>
    </div>
</form>