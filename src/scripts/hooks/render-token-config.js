export const RenderTokenConfig = {
    listen: () => {
        // Hooks.on("renderTokenConfig", (config, html, options) => {
        //     const actorLink = html.find("[name='actorLink']");
        //     actorLink.prop("disabled", true);
        //     actorLink.siblings()[0].outerHTML = "<label>Link Actor Data <span class='readable p10'>Unlinked actors are not supported by the system</span></label>";
        // });
    },
};
